<!DOCTYPE html>
<html>
    <head>
        <link href="https://unpkg.com/nes.css@2.3.0/css/nes.min.css" rel="stylesheet" />
        <link href="https://fonts.googleapis.com/css?family=Press+Start+2P" rel="stylesheet"/>
        <title>My To-Do App</title>
    </head>
    <body style="text-align:center; 
        border-style:solid; 
        border-width:15px 3px; 
        border-width: 6cm;
        border-color:bisque;
        background-color:blanchedalmond;
        ">
        <h1 style="text-align:center; text-decoration:underline;">TO-DO Practice</h1>
        <p>these are what i want to do:</p>
            {% for todo in my_variable %}
                <p style="{% if todo['Complete'] == 1 %} color:pink; text-decoration: line-through; {%endif%}">
                {{todo['description']}}
                {% if todo['Complete'] == 0 %}
                    <form action="/complete" method="post" class ="d-inline-block">
                        <i class="nes-icon trophy is-small"></i>
                        <input name="todo_id" value="{{todo['Id']}}" hidden>
                        <input type="submit" value="Complete" class="nes-btn" 
                            style="background-color:bisque; 
                            border-top-style:dashed; 
                            border-right-style:solid; 
                            border-bottom-style:dashed; 
                            border-left-style:solid;
                            font-size:12px;

                            ">
                        <i class="nes-icon trophy is-small"></i>
                            {% endif %}
                    </form action="/complete" method="post">
                </p>
            {% endfor %}
        <form action="/add" method="post" style="text-align:center;">
            <textarea name="new_todo" class="nes-textarea">
            </textarea>
            <input type="submit" class="nes-btn" style="background-color:bisque;">
        </form>
    </body>
</html>
